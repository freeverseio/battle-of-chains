import React from "react";
import { Blockchain } from "./types";

interface BlockchainQuadrantProps {
  blockchain: Blockchain;
}

const BlockchainQuadrant: React.FC<BlockchainQuadrantProps> = ({
  blockchain,
}) => {
  return (
    <div
      className="absolute w-1/2 h-1/2 border-2 border-[#8B4513] bg-opacity-20"
      style={{
        left: blockchain.x < 50 ? "0" : "50%",
        top: blockchain.y < 50 ? "0" : "50%",
      }}
    >
      <div
        className="absolute  text-[#8B4513] text-xl font-bold"
        style={{
          left: "50%",
          top: "50%",
          transform: "translate(-50%, -50%)",
        }}
      >
        {blockchain.name}
      </div>
    </div>
  );
};

export default BlockchainQuadrant;
